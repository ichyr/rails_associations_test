<%= form_for(@instruktor) do |f| %>
  <% if @instruktor.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@instruktor.errors.count, "error") %> prohibited this instruktor from being saved:</h2>

      <ul>
      <% @instruktor.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :name %><br>
    <%= f.text_field :name %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
